import 'package:flutter/material.dart';

class TopicsCategory {
  final String categoryTitle;
  final IconData categoryIcon;

  const TopicsCategory(
      {required this.categoryTitle, required this.categoryIcon});
}
